CREATE TABLE person (
    ID              INT           NOT NULL,
    NAME            TEXT          NOT NULL,
    AGE             INT           NOT NULL,
    ADDRESS_STREET  VARCHAR(50),
    ADDRESS_CITY    VARCHAR(50),
    ADDRESS_ZIP     INT,
    ADDRESS_COUNTRY VARCHAR(50)
);
